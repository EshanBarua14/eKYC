# M93 — Fix CORS + PEP 404 + Frontend
## Date: 2026-04
### Done
- CORS config fixed — frontend origins whitelisted
- PEP router double /v1 prefix fixed
- entries.filter frontend bug fixed
- Frontend working end-to-end
